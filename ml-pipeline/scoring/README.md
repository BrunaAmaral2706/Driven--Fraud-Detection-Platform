# Scoring Module

Feature extraction and risk score computation for fraud detection.

**Current:** Rule-based scoring via backend services.  
**Target:** scikit-learn / XGBoost model with feature pipeline.
